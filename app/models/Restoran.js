class Restoran {

    constructor({id, uid, name, url, kkmServerUrl}) {
        this.id = id
        this.uid = uid
        this.name = name
        this.url = url
        this.kkmServerUrl = kkmServerUrl
    }
}

module.exports = Restoran
