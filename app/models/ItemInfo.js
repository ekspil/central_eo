class ItemInfoDTO {

    constructor({id, uid, source, info}) {
        this.id = id
        this.uid = uid
        this.source = source
        this.info = info
    }
}

module.exports = ItemInfoDTO
